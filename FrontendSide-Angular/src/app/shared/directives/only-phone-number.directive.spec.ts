import { OnlyPhoneNumberDirective } from './only-phone-number.directive';

describe('OnlyPhoneNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyPhoneNumberDirective();
    expect(directive).toBeTruthy();
  });
});
