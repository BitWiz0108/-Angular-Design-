import { ShowOneTriggerDirective } from './show-one-trigger.directive';

describe('ShowOneTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOneTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
